<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE generatorConfiguration PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd" >
<generatorConfiguration>

    <!-- 配置文件 -->
    <properties resource="jdbc.properties"></properties>

    <!-- mysql驱动包 -->
    <classPathEntry
            location="D:\Software\mysql-connector-java-5.1.46\mysql-connector-java-5.1.46\mysql-connector-java-5.1.46.jar"/>

    <context id="context" targetRuntime="MyBatis3">

        <!-- 去掉注释 -->
        <commentGenerator>
            <property name="suppressAllComments" value="true"/>
        </commentGenerator>

        <!-- 数据库连接 -->
        <jdbcConnection driverClass="${jdbc.driver}"
                        connectionURL="${jdbc.url}"
                        userId="${jdbc.username}"
                        password="${jdbc.password}"/>

        <!-- model生成 -->
        <javaModelGenerator targetPackage="com.cjy.model" targetProject="src/main/java"/>

        <!-- MapperXML生成 -->
        <sqlMapGenerator targetPackage="com.cjy.mapper" targetProject="src/main/java"/>

        <!-- Mapper接口生成 -->
        <javaClientGenerator targetPackage="com.cjy.mapper" targetProject="src/main/java" type="XMLMAPPER"/>

        <!-- 需要映射的表 -->
        <table tableName="user" domainObjectName="User"></table>
        <table tableName="book" domainObjectName="Book"></table>
    </context>
</generatorConfiguration>